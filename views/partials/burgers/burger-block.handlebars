<li>
	{{name}}

	<button class="change-eat" data-id="{{id}}" data-newsleep="{{eaten}}">
		{{#if eaten}}Eat!{{else}}Barf!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
